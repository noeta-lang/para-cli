[package]
name = "para/cli"
version = "0.4.0"
# The minimum noeta this package works with; enforced at resolve time from noeta 0.2.1 on.
toolchain = ">=0.5"
license = "MIT OR Apache-2.0"
description = "Command-line apps where the function signature is the spec — parser, help text, and exit codes derived from #[Command] functions."
keywords = ["cli", "terminal", "commands", "arguments"]
